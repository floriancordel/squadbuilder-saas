// -----------------------------------------------------------------------------
// Copie ce fichier en `config.js` (même dossier) et remplis avec les valeurs de
// ton projet Supabase : dashboard Supabase > Project Settings > API.
//   - Project URL          -> SUPABASE_URL
//   - Project API keys : anon public -> SUPABASE_ANON_KEY
// La clé "anon" est PUBLIQUE (elle vit dans le navigateur), c'est normal et sûr :
// la sécurité des données est assurée par les policies RLS (voir supabase/schema.sql).
// Ne mets JAMAIS la clé "service_role" ici.
// -----------------------------------------------------------------------------
window.SUPABASE_URL = "https://VOTRE-REF.supabase.co";
window.SUPABASE_ANON_KEY = "VOTRE_CLE_ANON_PUBLIQUE";
